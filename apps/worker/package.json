{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "ts-node src/worker.ts",
    "dev": "ts-node-dev --respawn src/worker.ts",
    "test-loop": "ts-node src/test-loop.ts"
  },
  "dependencies": {
    "@ventry/automation": "workspace:*",
    "@ventry/db": "workspace:*",
    "@ventry/queue": "workspace:*",
    "bullmq": "^5.7.0",
    "dotenv": "^17.3.1",
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  }
}
